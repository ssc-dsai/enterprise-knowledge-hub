apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ekh-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ssc-dsai/enterprise-knowledge-hub.git
    targetRevision: main
    path: deployments/ekh
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: ekh-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
