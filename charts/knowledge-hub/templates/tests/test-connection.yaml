apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knowledge-hub.fullname" . }}-test-connection"
  labels:
    {{- include "knowledge-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knowledge-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
