apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-hub.fullname" . }}
  labels:
    {{- include "knowledge-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledge-hub.selectorLabels" . | nindent 4 }}
